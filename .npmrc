# WARNING: minimum-release-age=0 disables pnpm's security check for recent packages.
# This is required because the lockfile contains packages published within 16 hours.
# TODO: Revisit this setting and update lockfile to use older stable versions.
minimum-release-age=0
shamefully-hoist=true